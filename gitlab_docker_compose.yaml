services:
  gitlab:
    image: gitlab/gitlab-ee:latest
    container_name: gitlab
    restart: always

    hostname: v2202603345366442081.nicesrv.de

    ports:
      - "80:80"
      - "443:443"
      - "2222:22"

    volumes:
      - /srv/gitlab/config:/etc/gitlab
      - /srv/gitlab/logs:/var/log/gitlab
      - /srv/gitlab/data:/var/opt/gitlab

    shm_size: '256m'
